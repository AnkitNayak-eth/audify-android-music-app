<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Audify" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/custom_text_color</item>
        <item name="android:fontFamily">@font/audiowide</item>
    </style>

    <style name="Theme.Audify" parent="Base.Theme.Audify" />
</resources>